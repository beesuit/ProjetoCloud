import java.net.URL;

import com.xensource.xenapi.APIVersion;
import com.xensource.xenapi.Connection;
import com.xensource.xenapi.Session;
import com.xensource.xenapi.VM;


public class Teste {

	public Teste() {
		//Connection connection = new Connection(new URL("http://127.0.0.1"));
		//Session session = Session.loginWithPassword(connection, uname, pwd, APIVersion.latest().toString());
		//VM.getByNameLabel(c, label);
	}

}
